<?php
/** @var Oggetto\Simple\ViewModel\SimpleArray $viewModel */
$viewModel = $block->getViewModel();
?>

<a href="<?= $block->escapeUrl($block->getCustomUrl()) ?>">Go to google</a>

<select name="some_select" data-bind="options: someOptions,
                                      optionsText: 'label',
                                      optionsValue: 'value',
                                      value: selectedOption,
                                      scope: 'optionsList'"></select>

<div data-bind="style: {color: selectedOption().color }, scope: 'optionsList'">
    <span data-bind="text: selectedOption() ? selectedOption().label : 'Select smth pls', scope: 'optionsList'"></span>
</div>

<script type="text/x-magento-init">
{
    "*": {
        "Magento_Ui/js/core/app": {
            "components": {
                "optionsList": {
                    "component": "Oggetto_Simple/js/options-list",
                    "someOptions": <?= $viewModel->getJson() ?>
                }
            }
        }
    }
}
</script>
